class Deck:
    def __init__(self):
        self.deck = []

    def add_card(self, card):
        self.deck.append(card)

    def __str__(self):
        deck_str = ""
        for card in self.deck:
            deck_str += card.__str__() + "\n"

def main (self):
    card = Card(1, 0)
    return card

